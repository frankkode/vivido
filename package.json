{
  "name": "vivido-chess",
  "version": "1.0.0",
  "description": "Multiplayer chess game with streaming capabilities",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend && npm run postbuild",
    "postbuild": "mkdir -p backend/dist/public && cp -r frontend/dist/* backend/dist/public/ && echo 'Frontend files copied to backend/dist/public'",
    "start": "npm run start --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
